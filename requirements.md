# DummySqlGenerator 要件定義

## 概要

テーブル定義とカラム設定を入力し、INSERT文形式のダミーSQLを生成するWebツール。
フロントエンドのみで完結する静的サイトとして実装する。

## 技術スタック

| 項目 | 内容 |
|---|---|
| フレームワーク | React + TypeScript |
| ビルド | Vite |
| スタイリング | Tailwind CSS + shadcn/ui |
| ダミーデータ生成 | faker-js（ja ロケール） |
| ホスティング | GitHub Pages or Vercel |

## UI レイアウト

上下分割

```
┌──────────────────────────────────────┐
│ テーブル定義エリア                    │
│ [Table: users] [Table: orders] [+]   │
│ カラム設定...                         │
├──────────────────────────────────────┤
│ 出力エリア                            │
│ 件数: [100] TRUNCATE[□] [生成]        │
│ INSERT INTO ...                      │
└──────────────────────────────────────┘
```

---

## STEP1（MVP）

### テーブル定義

- 複数テーブルの追加・削除が可能

### カラム設定

各カラムに以下を設定できる：

| 設定項目 | 内容 |
|---|---|
| カラム名 | 任意の文字列 |
| 型 | 下記の対応型一覧を参照 |
| NULL可否 | ON/OFF |
| NULL生成割合 | NULL可の場合、0〜100% でユーザーが指定（デフォルト: 0%） |
| PK | ON/OFF（複数カラムへの設定で複合PKになる。組み合わせのユニーク性を保証） |
| FK | 参照先テーブル・PKカラムを指定（PKカラムへの参照のみ許可） |
| UNIQUE | ON/OFF |
| 固定選択肢 | ENUM は必須（未定義はバリデーションエラー）、VARCHAR は任意。定義した場合はその中からランダム生成 |
| 最大長 | VARCHAR の場合に設定（必須） |
| プレフィックス連番 | VARCHAR の場合に任意設定。プレフィックス文字列と連番桁数を指定すると `USR00001`, `USR00002` ... の形式で生成（1スタート、ゼロ埋め） |
| 精度・スケール | DECIMAL / FLOAT の場合に設定（例: 精度10, スケール2） |

### 対応型

- 数値：`TINYINT`（0〜127）, `INT`（0〜2,147,483,647）, `BIGINT`（0〜9,007,199,254,740,991）, `DECIMAL`, `FLOAT`
- 文字列：`VARCHAR`, `TEXT`
- 日付：`DATE`, `DATETIME`, `TIMESTAMP`
- その他：`BOOLEAN`, `ENUM`

### ダミーデータ生成

- カラム名から faker-js のメソッドを自動推測してマッピング（主要カラム名のみ対応: `name`, `email`, `phone`, `address` など）
- ユーザーが手動で上書き選択も可能

### FK 整合性

- FK は必ず参照先テーブルの PK カラムを指定する（非PK カラムへの参照は不可）
- 複数テーブル間の FK を考慮し、親テーブルの PK から値を引く
- 生成順序はトポロジカルソートで解決
- 循環参照が検出された場合は警告を表示し、該当 FK カラムの値は NULL で生成（生成は続行）

### 出力

- 形式：INSERT文のみ
- 日付型（`DATE`, `DATETIME`, `TIMESTAMP`）の生成範囲はユーザーが指定できる
- TRUNCATE オプション：先頭に `TRUNCATE TABLE` を付加できる
  - 選択時は独自モーダルダイアログで確認（誤操作防止）
- 件数上限：なし（将来的に検討）

### バリデーション

- 生成ボタン押下時のみ実行
- チェック項目：
  - PK未設定のテーブル
  - FK参照先テーブル・カラムが存在しない
  - FK参照先カラムが PK でない
  - ENUM の固定選択肢が未定義
  - UNIQUE カラムで生成件数がユニーク値の上限を超える

### データ保存

- テーブル定義を localStorage に自動保存
- ページリロード後も定義が復元される
- テーブル定義を全消去するリセットボタンを設置（誤操作防止のため確認ダイアログあり）

---

## テスト戦略

### 方針

- フレームワーク：Vitest
- 対象：ロジック層のみ（UIテストは対象外）

### テスト対象

| テスト対象 | 主なケース |
|---|---|
| INSERT文生成 | 形式・件数・型ごとの値の妥当性 |
| NULL生成 | 指定割合に沿っているか（統計的な確認） |
| プレフィックス連番 | ゼロ埋め・1スタート・重複なし |
| 固定選択肢 | 定義した選択肢の中からのみ生成されるか |
| UNIQUE制約 | 重複なし保証 |
| 複合PK | 組み合わせのユニーク性 |
| トポロジカルソート | 依存順の正確性 |
| 循環参照検出 | 正しく検出できるか |
| FK整合性 | 親テーブルのPK値から引けているか |
| バリデーション | 各エラーケースの検出 |

---

## STEP2（MVP後）

- DESCRIBE コマンドの出力結果からテーブル定義をインポート
- CSV / JSON 出力形式の追加
